import React from 'react';
import { Card, CardBody, Table } from 'reactstrap';

const Battery = props => {
  return (
    <Card>
      <CardBody></CardBody>
    </Card>
  );
};

export default Battery;
